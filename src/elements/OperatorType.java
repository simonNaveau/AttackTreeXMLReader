package elements;

/**
 * This enumeration represent all the different type that an Operator object can have
 * @author NAVEAU Simon
 */
public enum OperatorType {
    OR,
    AND,
    NOR,
    XOR
}
